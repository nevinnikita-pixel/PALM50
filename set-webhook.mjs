const t=process.env.TELEGRAM_BOT_TOKEN,s=process.env.TELEGRAM_WEBHOOK_SECRET,a=(process.env.APP_URL||'').replace(/\/$/,'');if(!t||!s||!a)throw new Error('Missing env');const r=await fetch(`https://api.telegram.org/bot${t}/setWebhook`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:`${a}/api/telegram-webhook`,secret_token:s,allowed_updates:['message','pre_checkout_query']})});console.log(await r.text());